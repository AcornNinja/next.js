import Link from 'next/link'

const linkStyle = {
    marginRight: 15
};

const Search = () => (
    <div>
       <input type="text"
              placeholder="Search"/>

        <button>Search</button>

    </div>
);
export default Search
